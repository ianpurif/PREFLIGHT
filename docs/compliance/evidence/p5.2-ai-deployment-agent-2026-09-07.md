# P5.2 AI deployment-agent evidence — 2026-09-07

## Evidence classification

- **Local positive integration:** real P5.2 controller + scripted deterministic model + deterministic
  public registry reader. This is not a live model, live positive Sepolia clearance, Speculos
  signature, or physical Ledger run.
- **Live negative integration:** real P5.2 controller + scripted deterministic model + read-only
  deployed Sepolia registry query. The existing fixture is deliberately unregistered.
- **Real provider architecture:** The official Google Gen AI (`@google/genai`) Gemini strict
  function-calling adapter is implemented and contract-tested with an injected SDK client. No
  `GEMINI_API_KEY` was available, so no external model execution is claimed.

No chain write, Ledger signing request, robot activation, P6 behavior, private envelope, blind,
credential, raw signature, raw user request text, or confidential runtime payload was used or
captured.

## Reproduction commands

```text
bun run --cwd apps/api evidence:p52-agent
bun run --cwd apps/api evidence:p52-agent:live-read
bun test apps/api/test/deployment-agent.test.ts apps/api/test/gemini-model.test.ts
```

## Trace A — unsafe build

Request: `Prepare AMR-17 unsafe Build A for Warehouse Manila-01`

```text
resolveDeploymentTarget → RESOLVED
getDeploymentContext → LOCKED
getEvaluationStatus → HOLD
getClearance → BLOCKED / CLEARANCE_NOT_FOUND
final → BLOCKED / EVALUATION_HOLD
Ledger → NOT_REQUESTED
```

No preparation nonce or Ledger handoff was produced.

## Trace B — corrected cleared build

Request: `Deploy AMR-17 v4.7.21 to Warehouse Manila-01`

```text
resolveDeploymentTarget → RESOLVED
getDeploymentContext → LOCKED
getEvaluationStatus → CLEAR
getClearance → ELIGIBLE
prepareDeploymentIntent → PREPARED
getLedgerAuthorizationStatus → AWAITING_HUMAN
final → LEDGER_APPROVAL_REQUIRED
```

Locked target:

- site: `site:warehouse-manila-01`
- robot: `robot:amr-17`
- build: `robot-build:amr-17-v4.7.21`
- build digest: `sha256:1111111111111111111111111111111111111111111111111111111111111111`
- P1 intent digest:
  `sha256:60b7cdd485f26a723e56c5324140f07261e9f37f8b169e058c8c2de5a7bc91af`
- EIP-712 digest:
  `0xbad758362236c0e32617352de644ecd867695d98df735864ddf7b8db4dc45ae8`

This trace stopped at the human boundary. The test suite separately signs that exact prepared
request with a test-only key, passes it through the real P5 signature recovery, postcheck, and atomic
nonce consumption, then confirms that—and only that—changes the agent status to `AUTHORIZED`.
This is cryptographic software evidence, not Ledger device evidence.

## Trace C — mutated exact build

Request: `Deploy mutated Build C for AMR-17 to Warehouse Manila-01`

The catalog locked `robot-build:amr-17-mutated-c` and digest
`sha256:3333333333333333333333333333333333333333333333333333333333333333`
while the candidate clearance remained bound to Build B.

```text
resolveDeploymentTarget → RESOLVED
getDeploymentContext → LOCKED
getEvaluationStatus → CLEAR
getClearance → ELIGIBLE for the supplied public clearance
prepareDeploymentIntent → BLOCKED / CLEARANCE_BINDING_MISMATCH
final → BLOCKED
Ledger → NOT_REQUESTED
```

This valid public form selects Build C; deterministic preparation still refuses to reuse Build B's
clearance. A second target or free-form conflicting text is now rejected before provider/audit use.

## Live Sepolia negative read

Captured at `2026-09-07T09:15:30.384Z`:

- chain ID: `11155111`
- registry: `0xFB270cc222efa8B5005AA097dD512Be2558dde65`
- block: `11653234`
- block hash: `0x35e6293c55ea4df200bfd8d50b0ec96d26fc98caaeb22ba48b1db89b9542a38e`
- exact existing fixture: `site:warehouse-a` / `robot:picker-01` /
  `robot-build:release-001`
- canonical public request: `Deploy release-001 for picker-01 to warehouse-a`
- result: `BLOCKED / CLEARANCE_NOT_FOUND`
- Ledger status: `NOT_REQUESTED`

This proves the agent can traverse its real read-only Sepolia tool seam and respect current missing
state. It does not prove the registry contains no other clearance and is not a positive live Build B
run.

## Adversarial result summary

Tests reject skipped/reordered/repeated tools, unknown `sign`, `/release/consume`, and registry-write
calls, alternate chain/registry fields, arbitrary authorization fields, signature reuse fields,
Unicode-confusable/missing/ambiguous aliases, injected `AUTHORIZED` claims, a conflicting second
target, a non-keyword restricted-geometry canary, malformed/duplicate provider calls, provider
failure, and credentialless fallback. The host compares raw input only against finite catalog forms,
then discards it; it sends/audits only a canonical public projection. In all cases deterministic
status remains blocked or Ledger-required; model output cannot create authorization.

## Ledger boundary

Production WebHID remains unchanged and default. Speculos remains development/test-only and is not
exercised by this evidence. The existing missing `GATING_TOKEN`, application origin/accepted
descriptor, Speculos A/B/E/F, and physical hardware blockers remain. Key Ring was not added.
