# Maps the fixed workflow secret ID to a local environment variable for CRE simulation.
# The value itself belongs in an ignored env file or the CRE Vault DON, never in this file.
secretsNames:
  PREFLIGHT_CONFIDENTIAL_EVALUATION_INPUT:
    - PREFLIGHT_CONFIDENTIAL_EVALUATION_INPUT_JSON
